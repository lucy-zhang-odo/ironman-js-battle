# 勇者 vs 小怪 —— 遊戲流程圖

Day 22 畫的。第一階段我需要一張速查表(記語法),第二階段需要的是這張圖(記流程)。

## 主流程

```
[開啟頁面]
    ↓
[讀取最少回合紀錄] ←─ localStorage
    ↓
[顯示初始畫面]
    ↓
[等待輸入勇者名字] ←──────────────┐
    ↓ 按「出征」                   │
[驗證名字]                        │
    ├─ 空值 / 只有空白 ──────────→┤ 顯示「請先輸入名字!」
    ├─ 超過 10 字 ───────────────→┘ 顯示「名字太長了」
    ↓ 通過
[設定 hero.name] ←─ 存原始輸入,顯示時才處理
    ↓
[準備就緒]
    ↓ 按「攻擊」
┌────────────── 戰鬥回合 ──────────────┐
│                                       │
│  [第一次攻擊?] ─是→ [鎖定名字輸入]     │
│       ↓ 否 / 鎖定完                    │
│  [isOver?] ─是→ [鞭屍計數 +1] → 結束   │
│       ↓ 否                             │
│  [計算傷害]                            │
│    base = max(1, atk - def)           │
│    damage = max(1, base + 亂數(-3~3)) │
│       ↓                                │
│  [扣除敵人血量]                         │
│       ↓                                │
│  [敵人 hp <= 0?]                       │
│    ├─ 否 → [回合 +1] ──────┐          │
│    └─ 是 → [血量歸零]       │          │
│            [isOver = true]  │          │
│            [按鈕變鞭屍]      │          │
│            [比對並存紀錄]    │          │
│                 ↓           │          │
│  [更新畫面] ←───────────────┘          │
│    - 回合數                            │
│    - 雙方血量文字 + 血條寬度            │
│    - 危險/死亡樣式                      │
│                                        │
└────────────────────────────────────────┘
    ↓ 按「再戰一次」
[重置狀態] ── 血量、回合、鞭屍數、isOver、battleStarted
[重置畫面] ── 按鈕文字/樣式、輸入框解鎖、訊息
    ↓
回到「準備就緒」(紀錄保留)
```

## 狀態欄位與誰會改它

| 欄位 | 誰會改 | 重置時要還原嗎 |
|---|---|---|
| `hero.name` | startGame | 否(保留名字) |
| `hero.hp` / `enemy.hp` | attack | **是** |
| `turn` | attack(未結束時 +1) | **是** |
| `whipCount` | attack(已結束時 +1) | **是** |
| `battleStarted` | attack(第一次) | **是**(Day 20 抓到的不一致) |
| `isOver` | attack(擊敗時) | **是**(Day 21 的 bug) |
| localStorage 紀錄 | saveRecordIfBetter | **否**(跨場保留) |

最後一欄是重點:**重置時要一項一項對照這張表**。Day 21 那個 bug 就是漏了 `isOver`。

## 三條容易漏掉的路徑

這三條是測試時要特別走的:

1. **驗證失敗 → 舊名字保留**:輸入空值按出征,`hero.name` 不該被清掉
2. **已結束後再攻擊 → 走鞭屍分支**:不扣血、不推進回合、不存紀錄
3. **再戰一次 vs 重開瀏覽器**:兩條路徑的結果必須一致

## 加入反擊與骰子之後的回合(Day 24–27)

```
                    [準備就緒]
                         |
        ┌────────────────┴────────────────┐
     按「攻擊」                        按「擲骰子」
        │                                 │
        │                          [冷卻中?] ─是→ 擋下,提示剩餘回合
        │                                 ↓ 否
  [算傷害(含蓄力 ×3)]              [擲 1~6,套用效果]
        ↓                            奇數 → 勇者(下回合起算)
  [扣小怪血]                         偶數 → 小怪(立即生效)
        ↓                                 ↓
  [小怪死了?] ─是→ [勝利]           [設定冷卻 3 回合]
        ↓ 否                              │
        └────────────┬────────────────────┘
                     ↓
          [小怪反擊](延後 380ms 演出)
                     ↓
          [勇者死了?] ─是→ [失敗]
                     ↓ 否
          [回合 +1] → [增益倒數,歸零的移除] → [冷卻 −1]
                     ↓
                回到「準備就緒」
```

**兩條路徑共用「小怪反擊」** —— 因為攻擊和擲骰都消耗勇者的回合,而小怪照樣行動。

### 增益的兩個關鍵規則

| 規則 | 說明 |
|---|---|
| 基礎值永遠不動 | `state.hero/enemy` 的 atk/def 從頭到尾不變,效果存在 `state.buffs` |
| 生效時機 | 勇者受益的 `pending`(下回合起算),小怪受益的立即生效 |

### 骰子六面

| 點數 | 受益 | 效果 | 類型 |
|---|---|---|---|
| 1 | 勇者 | 蓄力:下次攻擊 ×3 | 一次性 |
| 3 | 勇者 | 治療:回復 40 | 立即 |
| 5 | 勇者 | 破甲:小怪防禦 −5 / 3 回合 | 持續(下回合起) |
| 2 | 小怪 | 狂暴:攻擊 +5 / 3 回合 | 持續(立即) |
| 4 | 小怪 | 再生:回復 15 | 立即 |
| 6 | 小怪 | 硬化:防禦 +3 / 3 回合 | 持續(立即) |

## 狀態欄位(更新版)

| 欄位 | 重置時要還原嗎 |
|---|---|
| `hero.hp` / `enemy.hp` / `turn` / `whipCount` | 是 |
| `battleStarted` / `ready` / `isOver` / `result` | 是 |
| **`buffs`(清空)/ `charged` / `diceCooldown`** | **是** |
| localStorage 紀錄 | 否(跨場保留) |

## 尚未實作(Day 28 之後)

```
[敵人反擊]      ← 目前小怪只會站著挨打
[勇者陣亡判定]  ← 目前只有勝利,沒有失敗
[受擊震動]      ← Day 17 決定的表現層效果
[失敗結果畫面]
```

加入反擊之後,戰鬥回合會變成:

```
[玩家攻擊] → [敵人死了?] ─是→ [勝利]
                  ↓ 否
             [敵人反擊]
                  ↓
             [勇者死了?] ─是→ [失敗]
                  ↓ 否
             [回合 +1] → 等待下一次攻擊
```

**注意這裡多了一個判斷**:敵人死掉之後就不該反擊。順序錯了會變成「小怪死了還打你一下」。
